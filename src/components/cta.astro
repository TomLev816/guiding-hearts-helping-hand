---

export interface Props {
  page: string;
  microcopy: string;
  buttoncopy: string;
}

const { page, microcopy, buttoncopy } = Astro.props;
---

<section class="font-sans bg-[#e3efee] py-20 px-4 sm:px-6 lg:px-8 mb-20">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="font-serif text-4xl md:text-5xl text-[#393836] mb-4">
      Let’s Talk
    </h2>
    <p class="max-w-2xl mx-auto text-lg text-[#575654] leading-relaxed mb-10">
      {microcopy}
    </p>
    <a href="/contact" class="inline-block bg-[#004ba8] text-white font-bold text-lg px-8 py-4 rounded-lg hover:bg-[#0062db] transition-colors duration-300 shadow-md">
      {buttoncopy}
    </a>
  </div>
</section>
